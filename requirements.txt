# Web Framework
fastapi>=0.100.0
uvicorn[standard]
python-multipart

# AWS Lambda Adapter
mangum>=0.17.0

# HTTP Client (for API communication)
httpx>=0.26.0
aiohttp>=3.9.0
requests>=2.31.0

# Machine Learning - PyTorch and Transformers for local model inference
torch>=2.0.0
transformers>=4.30.0

# Data Processing
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0

# CORS
fastapi-cors

# Explainability (LIME for PhD research)
lime>=0.2.0.1

# NOTE: AWS Lambda Configuration
# - Memory: 3008 MB (sufficient for quantized model + PyTorch)
# - Timeout: 300 seconds (5 minutes for LIME processing)
# - Model loads directly from HuggingFace Hub into /tmp/model_cache
