# Web Framework
fastapi>=0.100.0
uvicorn[standard]
python-multipart

# HTTP Client (for HuggingFace API calls)
httpx>=0.26.0
aiohttp>=3.9.0
requests>=2.31.0

# Data Processing (minimal - only what LIME needs)
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0

# CORS
fastapi-cors

# Explainability (LIME for PhD research)
lime>=0.2.0.1

# NOTE: We do NOT install PyTorch, Transformers, or ONNX for API-only mode
# This keeps memory usage under 512MB for Render free tier
# The model runs on HuggingFace's servers via Inference API
