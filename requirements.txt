# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# HTTP Client (for HuggingFace API calls)
httpx>=0.26.0
aiohttp>=3.9.0
requests>=2.31.0

# Data Processing (minimal - no heavy ML stack)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.4.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging
loguru>=0.7.2

# CORS
fastapi-cors>=0.0.6

# Note: We no longer need:
# - torch (too heavy for Render free tier)
# - transformers (model is on HuggingFace API)
# - datasets
# - sentencepiece
# - safetensors
# - psutil
# - accelerate
# - lime (optional - can be added back for local explainability)

